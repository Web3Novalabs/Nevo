/**
 * Centralized image paths and settings for Nevo.
 * Use next/image for all images to get automatic optimization (AVIF/WebP, sizing, lazy load).
 * Prefer JPG for photos and large graphics for faster loading; use SVG for icons.
 * For fastest logo load, add public/logo.jpg and set LOGO_SRC to "/logo.jpg".
 */
export const LOGO_SRC = "https://nevo.app/logo.jpeg";
export const LOGO_WIDTH = 32;
export const LOGO_HEIGHT = 32;
